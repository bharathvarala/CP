#for all math related things lcm,gcd,primes
import math 

from collections import Counter,deque
#for dict of count deque to add and delete elements from both sides 

import heapq as h
# pq=[]
# h.heapify(pq)
# h.heappush(pq,(2,3))
# print(h.heappop(pq))

from sortedcontainers import SortedList, SortedSet, SortedDict
# s=SortedList()
# s.add(1)


